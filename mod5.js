console.log("Wussup Tho World");
users[1,12,3]



// Random Song & Random Song Section Function - On the music page when hovering ver a album card, the album begins to play a random song at a random section
    function randomSongPlayer(album) {
        let randomalbum = Math.floor(Math.random() * album.duration);
        let randomsong = album[randomalbum * Math.floor(Math.random(song.id)  * Math.floor(Math.random) * song.duration)];
        
        return randomsong
    }
// Form Processing Validation Function - Retrieve a user and activate returning user privs.
    function userList(user)
        const userlist = users//'[]
    function getUser(){
        if (user.name in userList(all))
            return returnUser()
    }
// Recepit Email Delivery

// Purchasing Functions - Functions that create and manage a cart for checkout
    function shopCart() {
        const cart = []
        function addToCart(item) {
            item.itemid + cart
        function takeFromCart(item) {
            item.itemid - cart
    } 
    }
}

    function items() {
        const item = {
            itemid: "",
            itemprice: "",
            itemzone:"",
        }
    }